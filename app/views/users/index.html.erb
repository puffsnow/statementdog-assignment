<table>
  <thead>
    <tr>
      <th>id</th>
      <th>name</th>
      <th>actions</th>
    </tr>
  </thead>
  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.id %></td>
        <td><%= user.name %></td>
        <td>
          <%= link_to '成為 user', become_user_path(user) %>
        </td>
      <tr>
    <% end %>
  </tbody>
</table>
